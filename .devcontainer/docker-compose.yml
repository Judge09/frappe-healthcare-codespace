version: "3.9"
services:
  frappe:
    image: frappe/bench:latest
    container_name: frappe-healthcare
    volumes:
      - .:/workspace
    command: sleep infinity
    ports:
      - "8000:8000"
      - "3306:3306"
    environment:
      - MYSQL_ROOT_PASSWORD=123
